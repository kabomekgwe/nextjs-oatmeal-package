# Docker Environment Variables
# Copy this file to .env.docker and update values for local development

# JWT Secret Key for WPGraphQL Authentication
# Generate a strong secret key for production
JWT_SECRET_KEY=your-local-development-secret-key-change-in-production

# WordPress Admin Credentials (for initial setup)
WP_ADMIN_USER=admin
WP_ADMIN_PASSWORD=admin123
WP_ADMIN_EMAIL=admin@example.com

# Database Configuration (must match docker-compose.yml)
DB_ROOT_PASSWORD=rootpassword
DB_NAME=wordpress
DB_USER=wordpress
DB_PASSWORD=wordpress

# Local Development URLs
WORDPRESS_URL=http://localhost:8080
NEXTJS_URL=http://localhost:3000